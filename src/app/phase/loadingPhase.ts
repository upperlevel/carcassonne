import {Phase} from "./phase";

export class LoadingPhase extends Phase {
    constructor() {
        super("loading");
    }

    enable() {
        super.enable();
    }

    disable() {
        super.disable();
    }
}
